# Examples

Examples are optional reference code and are not Alpha15 distributions or
workspace members. Keep them dependency-bounded and aligned with the public
kernel, Cordis, and native-plugin contracts.

The Alpha15 release bundle does not build examples. Use the package tests and
isolated install verifiers for release qualification.
