# LiteyukiBot WebUI

This is an optional, independently verified distribution. It is not installed
by `liteyukibot-v7` and is excluded from the CLI-first signed release bundle.
Install `liteyukibot-v7-webui[server]` explicitly when the frozen local WebUI
is required.
