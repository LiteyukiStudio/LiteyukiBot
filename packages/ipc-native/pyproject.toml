[build-system]
requires = ["maturin>=1.14,<2"]
build-backend = "maturin"

[project]
name = "liteyukibot-v7-ipc-native"
version = "7.0.0a13"
description = "Optional native primitives for Liteyuki IPC."
requires-python = ">=3.14"
license = "LicenseRef-LSO-Common-1.4"
license-files = ["LICENSE", "LICENSE.en", "LICENSE.zh-CN"]
authors = [{ name = "LiteyukiStudio" }]

[tool.maturin]
python-source = "python"
module-name = "liteyukibot_ipc_native._native"
manifest-path = "Cargo.toml"
bindings = "pyo3"
